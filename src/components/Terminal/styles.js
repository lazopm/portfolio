import { StyleSheet } from 'aphrodite';
import { bgDark } from 'constants/colors';

export default StyleSheet.create({
    Container: {
        background: bgDark, 
        height: 480,
        width: 640,
    }
});
