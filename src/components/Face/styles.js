import { StyleSheet } from 'aphrodite';

export default StyleSheet.create({
    Ascii: {
        margin: 0,
        fontSize: '8px',
        lineHeight: '4px',
        fontFamily: 'monospace',
    },
});
